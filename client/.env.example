# ── 클라이언트 전용 환경변수 ────────────────────────────────
# 이 파일을 복사하여 client/.env 로 저장 후 값을 채워주세요.
# 이 파일의 값은 서버가 읽어 /api/config 엔드포인트로 노출합니다.
# 민감한 정보(API 키 등)는 절대 여기에 넣지 마세요.

# 클라이언트가 요청을 보낼 서버 주소
# 개발 환경에서는 서버가 클라이언트를 직접 서빙하므로 빈 문자열(상대경로) 사용
# 클라이언트와 서버를 별도 도메인에 배포할 경우 전체 URL 입력
API_BASE_URL=
